Data and datasets
=================

``mlcg.data`` prodivides the classes and objects to define a molecule, while ``mlcg.dataset`` provides 
datasets compatible with pytorch to enable training over datasets with several configurations
of different molecules. 

=======

.. toctree::

   atom_data
   fixed_datasets
   h5_datasets
